cloud:
  aws:
    credentials:
      accessKey: accesskey
      secretKey: secretkey
    s3:
      bucket: fullstack-app-dev-app-o4831nka
    secretsmanager:
      secret-name: placeholder

spring:
  datasource:
    url: jdbc:mysql://fullstack-app-dev-nlb-5bd981aaae377569.elb.us-east-1.amazonaws.com:3306/anirudhrevalli
    username: root
    password: password

    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
